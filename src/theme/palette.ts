import { PaletteOptions } from '@mui/material';
import { FONT_COLOR, PRIMARY_COLOR } from '../consts/colors';

export const palette: PaletteOptions = {
  primary: {
    main: PRIMARY_COLOR,
  },
  text: {
    primary: FONT_COLOR,
  },
};
