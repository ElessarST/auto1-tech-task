import React from 'react';
import { styled } from '@mui/material';

const LogoImg = styled('img')({
  height: 30,
});

const Logo = () => (
  <LogoImg src="https://auto1-js-task-api--mufasa71.repl.co/images/logo.png" />
);

export default Logo;
